angular.module('nproxy', [ 'nproxy.controllers', 'nproxy.services']).config(function() {


/*
    $stateProvider
        .state('home', {
            url: "/home",
            //templateUrl: "home.html",
            controller: 'post-controller'
        })
        .state('posts', {
            url: "/posts/:post/",
            //templateUrl: "home.html",
            controller: 'post-controller'
        })


    // if none of the above are matched, go to this one
    $urlRouterProvider.otherwise("/home");*/
})